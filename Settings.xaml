<Page x:Class="PasswordManager.Settings"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PasswordManager"
      mc:Ignorable="d" 
      d:DesignHeight="134" d:DesignWidth="200"
      Title="Settings">

    <Grid VerticalAlignment="Bottom">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="26"/>
            <RowDefinition Height="26"/>
            <RowDefinition Height="26"/>
            <RowDefinition Height="26"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="167"/>
        </Grid.ColumnDefinitions>
        <CheckBox x:Name="alphabetUpperCaseLetters" Content="A-Z" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Height="15" Width="39"/>
        <CheckBox x:Name="alphabetLowerCaseLetters" Content="a-z" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Grid.Row="1" Height="15" Width="36"/>
        <CheckBox x:Name="digits" Content="0-9" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Grid.Row="2" Height="15" Width="38"/>
        <CheckBox Content="Специальные символы" HorizontalAlignment="Center" Grid.Row="3" VerticalAlignment="Center" Height="16" Width="147"/>
        <Button x:Name="save" Content="Сохранить" HorizontalAlignment="Left" Margin="80,0,0,0" Grid.Row="4" VerticalAlignment="Center"/>

    </Grid>
</Page>
